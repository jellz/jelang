fn int abc(str a, int b) {
	int c = 10;
	// int c = "not a number" // this will error due to assigning string to int (this is a comment!)
	str hello = "Hello //World"!"; // you can still use // in strings however
	bool foo = TRUE;
	functionCallHere("   This is a string ", TRUE);
	return "string here"; // this is not supposted to work
}